# 2026-09-18

## 产品矩阵域名重构 + 简历页
- 用户要求把产品矩阵从 6 个旧子域（skime/guard/term/dvsu/pulse/blog）缩减为 4 个真实产品 + blog，依据 `D:/Desktop/Project_Group/Center/项目总览说明.md`：
  - skime.neftoo.xyz → Skime 超级智能体平台（保留）
  - mawork.neftoo.xyz → Mawork 个人 AI 工作操作系统
  - ticketsys.neftoo.xyz → Ticketsys 门店促销员工单系统
  - crdb.neftoo.xyz → CRDB 医药连锁销售数据系统
  - blog.neftoo.xyz → Notebook 写作
- 重写 `index.html` 与 `products.html`：首页/产品页矩阵替换为 4 产品 + blog，更新筛选器（All/Agent/Work OS/Ticketing/Data/Journal）、域名索引、meta 描述、hero 文案。
- 批量更新 about/blog/research/contact/work 共 5 页：页脚 Matrix 列表（6→5）、通用描述文案、导航与 Site 列新增「简历」、Site 列新增 GitHub（https://github.com/markbunee）。清理 work.html 中 guard/term/pulse 失效域名标签、research.html 的 dvsu 失效域名。
- 新增 `resume.html`（独立简历页，Neftoo 极简编辑风双栏重排，含打印/导出 PDF 样式与「打印」按钮），内容来自用户更新版简历 `D:/Desktop/简历/马炫轩_AI产品简历.html`（华润三九/金域医学/智算/米内网 4 段经历 + 2 项目 + 技能/荣誉）。导航与页脚均链接。
- 验证：8 个 HTML 全部 200、HTML 结构校验 0 错误、无残留旧域名、博客中文 Markdown 经服务可达。

## 备注
- 预览服务：`python -m http.server 8123 --bind 127.0.0.1`（博客需走 http 而非 file://）。
- 设计系统沿用 styles.css（纸白+细线+赭红），简历页用内联 <style> 作用域样式，未改全局 CSS。

## 华润经历补全 + 人生时间线 + 去重
- 依据 `D:/Desktop/Project_Group/Mawork/workspaces/Core/2026年综合管理/2023.09-2026.08个人三年发展总复盘暨2026-2029三年战略规划.md`：
- `work.html` 时间线**新增华润三九条目**（2026.06—至今，KA 管培生），强调「业务+AI 双线 + 业务数据库」（大参林/高济/海王三大 KA 本地数据库 → CRDB 雏形）；同步把 lead「三段实习、四个项目」改为「四段经历、五个项目」、meta「7 entries」→「8 entries」。
- `resume.html` 华润条目补第 4 条「业务数据库」，点出 CRDB 关联（crdb.neftoo.xyz）。
- `about.html` **重写「成长轨迹」时间线**为一条人生时间线，消除与 work.html 的重复：大学之前（读书会/学生会/科幻写作/英语赛事，缩写）→ 第一个三年计划（2023–2026，大一/大二/大三三阶段）→ 第二个三年计划（2026–2029，站稳/深耕/冒尖三阶段，目标画像）。用新增 CSS 类 `.phases/.phase/.phase-y/.arc-note`（styles.css 已加，响应式 640px 单列）。
- 验证：9 个页面/资源 200；work.html 含华润/CRDB（2 处）、about.html 含三阶段（3 处）、"三段实习"与旧重复条目均已清除（0）；HTML 结构校验 0 错误。

## 红点隐藏 + 时间线倒序
- `styles.css` 把 `.brand .dot`（导航 logo 前 5px 赭红圆点）改为 `display:none`，全站 logo 不再有红点（无独立 favicon 文件）。
- `about.html`「成长轨迹」时间线**倒序**：原来「大学之前 → 第一个三年 → 第二个三年」（旧在上），改为「第二个三年（进行中，最新）→ 第一个三年（已走过）→ 大学之前（最底）」，符合"最新在最上、大学之前在最下"。
- 验证：红点规则 display:none；about 时间线 entry 顺序正确；HTML 结构校验 OK。

## favicon 红点（浏览器标签栏）移除
- 站点的 favicon 是各 HTML `<head>` 内联 SVG data URI：米白底 + 赭红圆点 `fill='#b8461f'`，即浏览器标签页上看到的红点。
- 将 8 个 HTML 的 favicon 统一替换为干净「N」字标（米白圆角底 `#faf9f6` + 墨色 `#1a1a1a` 描边 N，无红点），与无红点 logo 保持一致。
- 验证：favicon 内无 `#b8461f` 红点；8 页均有新 N 标；SVG 经 minidom 解析 8/8 有效。
