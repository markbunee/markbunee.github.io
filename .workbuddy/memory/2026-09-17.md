# 2026-09-17

## 网站整体重做（Neftoo 品牌化）
- 将 markbunee.github.io 从旧版个人站重构为 **Neftoo** 品牌极简编辑风站点。
- 设计系统：纸白底(`#faf9f6`)+ 细线 + 单色点缀(赭红 `#b8461f`)；字体 Inter + Instrument Serif；响应式。全部写在 `styles.css`。
- 交互脚本 `script.js` 重写：滚动进度条、移动端导航、揭示动画、产品矩阵筛选、复制按钮、Blog（fetch Markdown + 自写轻量 Markdown→HTML 渲染器、hash 路由 `#p=文件名`）。
- 页面重编排，删除旧 `internship.html / projects_Tech.html / projects_Business.html`，新建：
  - `index.html` 首页、`products.html` 产品矩阵、`work.html` 经历、`research.html` 研究、`blog.html` 博客、`about.html`、`contact.html`
- 主导航品牌为 Neftoo，产品矩阵统一挂 `neftoo.xyz` 子域：skime / guard / term / dvsu / pulse + blog.neftoo.xyz。
- Blog 数据在 `blog_file/`，`index.json` 索引 2 篇：`Concise manual of design and technology.md`、`我们应该保有信心，没有条件创造条件.md`（中文名文件经 Python 实测可经 http.server 正确读取）。
- 本地预览服务：`python -m http.server 8123 --bind 127.0.0.1`（博客 fetch 需经 http 而非 file://）。

## 备注
- 之前因 API 频率限制(429)中断，经核查所有文件均已完整写入，未受影响。
- 站点不依赖 `images/` 资源（改用 SVG 圆点 logo + 排版），无失效引用。
